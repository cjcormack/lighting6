//Boilerplate for the NK plugin
repositories {
    mavenLocal()
    mavenCentral()
    maven {
        url "http://127.0.0.1:8080/netkernel-maven"
        allowInsecureProtocol = true
    }
}

dependencies {
    classpath group: 'urn.org.netkernel', name: 'gradle.plugin', version: '[1.1.30,)'
}
